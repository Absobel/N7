{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build my project",
            "command": "rm -f ecoute *.c2s *.s2c && make",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
